const $=id=>document.getElementById(id);const intro=$('intro'),video=$('introVideo'),resume=$('resumeIntro'),envelopeScreen=$('envelopeScreen'),envelope=$('envelope'),profile=$('profile'),hq=$('hq'),error=$('formError'),splash=$('quickSplash');let finished=false,opened=false,authMode='create',authBusy=false;
const store={get:(k,d=null)=>{try{return JSON.parse(localStorage.getItem(k))??d}catch{return d}},set:(k,v)=>localStorage.setItem(k,JSON.stringify(v)),del:k=>localStorage.removeItem(k)};
const cases={free:[['الغرفة 317','جريمة غامضة'],['آخر اتصال','اختفاء'],['الشاهد الصامت','سرقة'],['الرسالة السوداء','ابتزاز'],['الممر الأخير','اختفاء'],['الملف المحذوف','تجسس']],premium:[['الهوية المزدوجة','انتحال'],['خيانة منتصف الليل','خيانة'],['الدائرة المغلقة','جريمة غامضة'],['العميل السابع','تجسس'],['الصوت المفقود','اختفاء'],['النسخة الأخيرة','سرقة']]};
function renderCases(){['free','premium'].forEach(t=>{const el=$(t+'Cases');el.innerHTML=cases[t].map((c,i)=>`<button class="case-card" data-case="${t}-${i}"><b>${c[0]}</b><small>${c[1]} · ${t==='free'?'6':'10'} لاعبين</small></button>`).join('')})}
function chooseHQ(){const r=innerHeight/Math.max(1,innerWidth),opts=[[16/9,'hq-9x16.png'],[18/9,'hq-9x18.png'],[19.5/9,'hq-9x19_5.png'],[20/9,'hq-9x20.png'],[21/9,'hq-9x21.png']];return opts.reduce((a,b)=>Math.abs(b[0]-r)<Math.abs(a[0]-r)?b:a)[1]}
function preloadHQ(){const src='hq/'+chooseHQ();const im=new Image();im.src=src;$('hqBackground').src=src}
function hideAll(){[intro,envelopeScreen,profile,hq,splash].forEach(x=>x&&(x.hidden=true))}
function showHQ(name){hideAll();hq.hidden=false;$('hqName').textContent=name||store.get('investigator_name','المحقق');preloadHQ();renderCases()}
function showEnvelope(){if(finished)return;finished=true;try{video.pause()}catch{};store.set('intro_seen',true);intro.classList.add('intro-exit');setTimeout(()=>{intro.hidden=true;envelopeScreen.hidden=false},420)}
function startIntro(force=false){finished=false;intro.classList.remove('intro-exit');hideAll();intro.hidden=false;if(force)store.set('intro_seen',false);try{video.currentTime=0;video.muted=false;video.volume=1;const p=video.play();p?.catch(()=>resume.hidden=false)}catch{resume.hidden=false}}
function openEnvelope(){if(opened)return;opened=true;envelope.classList.add('opening');setTimeout(()=>envelope.classList.add('depart'),1200);setTimeout(()=>{envelopeScreen.hidden=true;profile.hidden=false},1800)}
function setAuthMode(mode){authMode=mode;error.textContent='';const isLogin=mode==='login';document.querySelector('.identity-form h1').textContent=isLogin?'تسجيل دخول المحقق':'إنشاء هوية المحقق';$('playerName').closest('label').hidden=isLogin;$('playerPasswordConfirm').closest('label').hidden=isLogin;$('createProfile').textContent=isLogin?'تسجيل الدخول':'اعتماد هويتي';$('existingAccount').textContent=isLogin?'إنشاء هوية جديدة':'لدي هوية بالفعل — تسجيل الدخول'}
function boot(){preloadHQ();const name=store.get('investigator_name');if(store.get('intro_seen',false)){hideAll();splash.hidden=false;setTimeout(()=>name?showHQ(name):(splash.hidden=true,profile.hidden=false),1250)}else startIntro()}
function nativeReady(){return typeof NativeAuth!=='undefined'}
function friendlyAuthError(msg=''){const m=String(msg||'');if(/password|credential/i.test(m))return'بيانات الدخول غير صحيحة أو كلمة المرور غير صالحة.';if(/email.*already|already.*use/i.test(m))return'هذا البريد مستخدم بحساب آخر.';if(/network/i.test(m))return'تعذر الاتصال بالشبكة. حاول مرة أخرى.';if(/cancel/i.test(m))return'تم إلغاء تسجيل الدخول.';return m||'تعذر إكمال تسجيل الدخول. حاول مرة أخرى.'}
window.onNativeAuthResult=r=>{authBusy=false;$('createProfile').disabled=false;$('googleAuth').disabled=false;if(!r||!r.ok){if(r?.action!=='restore')error.textContent=friendlyAuthError(r?.message);return}if(r.action==='logout'){store.del('investigator_name');store.del('profile_cache');setAuthMode('login');hideAll();profile.hidden=false;return}const fallback=$('playerName').value.trim()||'المحقق';const name=(r.name||fallback).trim();store.set('investigator_name',name);store.set('profile_cache',{name,email:r.email||$('playerEmail').value.trim(),uid:r.uid||''});showHQ(name)};
video.addEventListener('ended',showEnvelope);video.addEventListener('error',()=>{resume.hidden=false;resume.textContent='تعذر تشغيل المقدمة — اضغط للمتابعة';resume.onclick=showEnvelope});video.addEventListener('playing',()=>resume.hidden=true);$('skipIntro').onclick=showEnvelope;resume.onclick=()=>video.play();envelope.onclick=openEnvelope;
$('createProfile').onclick=()=>{if(authBusy)return;const name=$('playerName').value.trim(),email=$('playerEmail').value.trim(),p=$('playerPassword').value,c=$('playerPasswordConfirm').value;error.textContent='';if(authMode==='create'&&name.length<2)return error.textContent='اكتب اسم محقق من حرفين على الأقل.';if(!/^\S+@\S+\.\S+$/.test(email))return error.textContent='تأكد من البريد الإلكتروني.';if(p.length<8)return error.textContent='كلمة المرور يجب أن تكون 8 أحرف على الأقل.';if(authMode==='create'&&p!==c)return error.textContent='كلمتا المرور غير متطابقتين.';if(!nativeReady())return error.textContent='خدمة تسجيل الدخول غير متاحة في هذه النسخة.';authBusy=true;$('createProfile').disabled=true;$('googleAuth').disabled=true;error.textContent='جارٍ التحقق...';if(authMode==='create')NativeAuth.createAccount(name,email,p);else NativeAuth.signInEmail(email,p)};
$('googleAuth').onclick=()=>{if(authBusy)return;if(!nativeReady())return error.textContent='خدمة Google غير متاحة في هذه النسخة.';authBusy=true;$('createProfile').disabled=true;$('googleAuth').disabled=true;error.textContent='جارٍ فتح Google...';NativeAuth.signInGoogle()};
$('existingAccount').onclick=()=>setAuthMode(authMode==='create'?'login':'create');
$('hqSettings').onclick=()=>$('settingsSheet').hidden=false;$('closeSettings').onclick=()=>$('settingsSheet').hidden=true;$('replayIntro').onclick=()=>{$('settingsSheet').hidden=true;opened=false;startIntro(true)};
window.addEventListener('resize',()=>{if(!hq.hidden)preloadHQ()},{passive:true});document.addEventListener('click',e=>{const c=e.target.closest?.('.case-card');if(c){store.set('last_case',c.dataset.case);c.animate([{transform:'scale(.98)'},{transform:'scale(1)'}],{duration:140})}});boot();