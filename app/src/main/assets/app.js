const $=id=>document.getElementById(id);
const intro=$('intro'),video=$('introVideo')||{pause(){},play(){return Promise.resolve()},load(){},addEventListener(){},removeAttribute(){},muted:true,volume:0,currentTime:0},resume=$('resumeIntro')||{hidden:true,textContent:'',onclick:null},envelopeScreen=$('envelopeScreen'),envelope=$('envelope'),profile=$('profile'),hq=$('hq'),error=$('formError'),splash=null;
let finished=false,opened=false,authMode='login',authBusy=false,pendingGoogleProfile=null;
window.VEILMARK_PERF={bootStart:performance.now()};
const store={get:(k,d=null)=>{try{return JSON.parse(localStorage.getItem(k))??d}catch{return d}},set:(k,v)=>localStorage.setItem(k,JSON.stringify(v)),del:k=>localStorage.removeItem(k)};
let appDialogResolve=null;
function closeAppDialog(value){const d=$('appDialog');if(d){d.hidden=true;d.setAttribute('aria-hidden','true')}const r=appDialogResolve;appDialogResolve=null;if(r)r(value)}
function showAppDialog(message,{title='تأكيد',ok='تأكيد',cancel='إلغاء',danger=false,notice=false}={}){const d=$('appDialog');if(!d)return Promise.resolve(false);$('appDialogTitle').textContent=title;$('appDialogMessage').textContent=message;const okBtn=$('appDialogOk'),cancelBtn=$('appDialogCancel');okBtn.textContent=ok;cancelBtn.textContent=cancel;cancelBtn.hidden=notice;okBtn.classList.toggle('danger-dialog',danger);d.hidden=false;d.setAttribute('aria-hidden','false');return new Promise(resolve=>{appDialogResolve=resolve})}
window.appConfirm=(message,opts={})=>showAppDialog(message,opts);window.appNotice=(message,opts={})=>showAppDialog(message,{...opts,notice:true,ok:opts.ok||'حسنًا'});
function requestDeletePassword(){return new Promise(resolve=>{let d=document.getElementById('deletePasswordDialog');if(!d){d=document.createElement('div');d.id='deletePasswordDialog';d.className='app-dialog';d.innerHTML='<div class="app-dialog-card"><div class="app-dialog-mark">◈</div><h3>تأكيد هوية الحساب</h3><p>أدخل كلمة مرور حسابك لإكمال الحذف النهائي.</p><input id="deletePasswordInput" type="password" autocomplete="current-password" placeholder="كلمة المرور" style="width:100%;box-sizing:border-box;margin-top:16px;min-height:48px;border-radius:12px;border:1px solid #ffffff22;background:#090c0e;color:#fff;padding:0 14px"><div class="app-dialog-actions"><button id="deletePasswordCancel" class="app-dialog-btn secondary">إلغاء</button><button id="deletePasswordOk" class="app-dialog-btn primary-dialog danger-dialog">متابعة الحذف</button></div></div>';document.body.appendChild(d)}d.hidden=false;const input=document.getElementById('deletePasswordInput');input.value='';setTimeout(()=>input.focus(),50);document.getElementById('deletePasswordCancel').onclick=()=>{d.hidden=true;resolve('')};document.getElementById('deletePasswordOk').onclick=()=>{const v=input.value;d.hidden=true;resolve(v)}})}

$('appDialogCancel').onclick=()=>closeAppDialog(false);$('appDialogOk').onclick=()=>closeAppDialog(true);$('appDialog').addEventListener('click',e=>{if(e.target===$('appDialog'))closeAppDialog(false)});

const cases={free:[['ليلة الفندق','عيش الدور'],['حفلة القصر','تجربة اجتماعية'],['وردية المستشفى','دور تفاعلي'],['سوق منتصف الليل','عالم حي'],['رحلة الميناء','تجربة جماعية'],['موعد المقهى','قصة مفتوحة']],premium:[['ليلة القصر','دور خاص'],['الحجز الأخير','تجربة خاصة'],['سر الميناء','قصة تفاعلية'],['الحارس الليلي','دور متقدم'],['ضيف غير متوقع','عالم حي'],['الليلة الطويلة','تجربة جماعية']]};
function renderCases(){['free','premium'].forEach(t=>{const el=$(t+'Cases');if(el.dataset.ready)return;el.innerHTML=cases[t].map((c,i)=>`<button class="case-card" data-case="${t}-${i}"><b>${c[0]}</b><small>${c[1]} · ${t==='free'?'6':'10'} لاعبين</small></button>`).join('');el.dataset.ready='1'})}
function warmImage(src){const im=new Image();im.decoding='async';im.src=src}function idle(fn,delay=0){const go=()=>('requestIdleCallback'in window?requestIdleCallback(fn,{timeout:900}):setTimeout(fn,0));delay?setTimeout(go,delay):go()}function nativeReady(){return typeof NativeAuth!=='undefined'}function warmNativeAuth(){if(!nativeReady()||typeof NativeAuth.warmAuth!=='function')return;try{NativeAuth.warmAuth()}catch{}}function warmOnboarding(){idle(()=>{warmImage('media/envelope-closed.webp');warmImage('media/envelope-open.webp')},450);idle(warmNativeAuth,700)}function hideAll(){[intro,envelopeScreen,profile,hq,splash].forEach(x=>x&&(x.hidden=true))}
function stopSceneAudio(){try{video.pause();video.muted=true;video.volume=0;video.currentTime=0}catch{}}function resumeCurrentSceneAudio(){if(!intro.hidden&&!finished){try{video.muted=false;video.volume=1;const p=video.play();p?.catch(()=>resume.hidden=false)}catch{resume.hidden=false}}}window.stopSceneAudio=stopSceneAudio;window.onNativeAppPause=stopSceneAudio;window.onNativeAppResume=resumeCurrentSceneAudio;document.addEventListener('visibilitychange',()=>document.hidden?stopSceneAudio():resumeCurrentSceneAudio());
function setCaseView(view){const next=view==='premium'?'premium':'free';hq.dataset.caseView=next;document.querySelectorAll('.case-tab').forEach(b=>b.classList.toggle('active',b.dataset.view===next));const zone=document.querySelector('.case-zone');if(zone)zone.scrollTop=0}
function showHQ(name){stopSceneAudio();hideAll();hq.hidden=false;if(typeof NativeSocial!=='undefined'&&typeof NativeSocial.startRealtime==='function'){try{NativeSocial.startRealtime()}catch{}}const currentName=name||store.get('investigator_name','اللاعب');$('hqName').textContent=currentName;renderCases();setCaseView(hq.dataset.caseView||'free');const un=store.get('investigator_username','');if(nativeReady()&&un&&typeof NativeAuth.syncLocalProfile==='function'){try{NativeAuth.syncLocalProfile(currentName,un)}catch{}}window.VEILMARK_PERF.hqVisible=performance.now()}window.showHQ=showHQ;
function showEnvelope(){if(finished)return;finished=true;stopSceneAudio();store.set('intro_seen',true);envelopeScreen.hidden=false;envelopeScreen.classList.remove('visible');requestAnimationFrame(()=>envelopeScreen.classList.add('visible'));intro.classList.add('intro-exit');warmNativeAuth();setTimeout(()=>{intro.hidden=true;intro.classList.remove('intro-exit')},170)}
function startIntro(force=false){finished=false;opened=false;pendingGoogleProfile=null;envelope.classList.remove('opening','depart');envelopeScreen.classList.remove('visible');profile.classList.remove('ready');hideAll();intro.hidden=false;if(force)store.set('intro_seen',false);warmOnboarding();requestAnimationFrame(()=>{try{video.currentTime=0;video.muted=false;video.volume=1;const p=video.play();p?.catch(()=>resume.hidden=false)}catch{resume.hidden=false}})}
function openEnvelope(){if(opened)return;opened=true;stopSceneAudio();envelope.classList.add('opening');warmNativeAuth();setTimeout(()=>envelope.classList.add('depart'),120);setTimeout(()=>{profile.hidden=false;profile.classList.add('ready');setAuthMode('login')},150);setTimeout(()=>{envelopeScreen.hidden=true;envelopeScreen.classList.remove('visible')},380)}
function setAuthMode(mode){authMode=mode;error.textContent='';const login=mode==='login',googleSetup=mode==='googleSetup';const nameL=$('playerName').closest('label'),userL=$('usernameLabel'),emailL=$('playerEmail').closest('label'),passL=$('playerPassword').closest('label');document.querySelector('.identity-form h1').textContent=googleSetup?'إكمال هويتك':(login?'تسجيل الدخول':'إنشاء هويتك');document.querySelector('.identity-form p').textContent=googleSetup?'اختر اسم شخصيتك واسم مستخدم فريد لحسابك.':(login?'ادخل إلى حسابك وواصل عيش الدور.':'اسم شخصيتك يظهر للاعبين، واسم المستخدم فريد لحسابك.');nameL.hidden=login;userL.hidden=login;emailL.hidden=googleSetup;passL.hidden=googleSetup;$('googleAuth').hidden=googleSetup;document.querySelector('.or').hidden=googleSetup;$('existingAccount').hidden=googleSetup;$('createProfile').textContent=googleSetup?'اعتماد الهوية':(login?'دخول إلى عالمك':'إنشاء الحساب');if(!googleSetup){$('existingAccount').textContent=login?'إنشاء حساب جديد':'لدي حساب — تسجيل الدخول'}if(googleSetup){$('playerName').value='';$('playerUsername').value='';setTimeout(()=>$('playerName').focus(),50)}warmNativeAuth()}
function showGoogleSetup(r){pendingGoogleProfile={email:r.email||'',uid:r.uid||''};stopSceneAudio();hideAll();profile.hidden=false;profile.classList.add('ready');setAuthMode('googleSetup')}
function boot(){const name=store.get('investigator_name');renderCases();stopSceneAudio();hideAll();if(name)showHQ(name);else{profile.hidden=false;profile.classList.add('ready');setAuthMode('login');idle(warmNativeAuth,100)}}
function friendlyAuthError(msg=''){const m=String(msg||'');if(/requires recent authentication|recent login/i.test(m))return'لأمان حسابك، سجّل الخروج ثم ادخل من جديد قبل الحذف.';if(/operation.*not.*allowed|provider.*disabled|sign-in-provider.*disabled/i.test(m))return'طريقة تسجيل الدخول غير مفعلة في مشروع Firebase المرتبط بهذا الإصدار.';if(/client is offline|offline/i.test(m))return'تعذر الوصول إلى قاعدة البيانات الآن. تحقق من الاتصال وحاول مجددًا.';if(/username.*taken|اسم المستخدم.*مستخدم/i.test(m))return'اسم المستخدم مستخدم بالفعل. اختر اسمًا آخر.';if(/password|credential/i.test(m))return'بيانات الدخول غير صحيحة أو كلمة المرور غير صالحة.';if(/email.*already|already.*use/i.test(m))return'هذا البريد مستخدم بحساب آخر.';if(/network/i.test(m))return'تعذر الاتصال بالشبكة. حاول مرة أخرى.';if(/cancel/i.test(m))return'تم إلغاء تسجيل الدخول.';return m||'تعذر إكمال العملية.'}
function clearAccountLocal(){store.del('investigator_name');store.del('investigator_username');store.del('profile_cache');pendingGoogleProfile=null}
function setAuthBusy(on,label=''){authBusy=on;const primary=$('createProfile'),google=$('googleAuth');primary.disabled=on;google.disabled=on;primary.classList.toggle('busy',on&&label!=='google');google.classList.toggle('busy',on&&label==='google');if(on&&label!=='google')error.textContent=label==='setup'?'جارٍ اعتماد الهوية…':'جارٍ التحقق…';else if(!on)error.textContent=''}
window.onNativeAuthResult=r=>{setAuthBusy(false);if(!r||!r.ok){if(r?.action==='delete'){if(r?.message==='REAUTH_PASSWORD'){requestDeletePassword().then(p=>{if(p)NativeAuth?.reauthenticatePasswordAndDelete?.(p)});return}window.appNotice?.(friendlyAuthError(r?.message),{title:'تعذر حذف الحساب'});return}if(r?.action==='sync'&&r?.message==='USERNAME_CONFLICT'){const n=store.get('investigator_name','اللاعب');store.del('investigator_username');showGoogleSetup({});$('playerName').value=n;setTimeout(()=>error.textContent='اسم المستخدم الحالي مستخدم بحساب آخر. اختر اسم مستخدم جديدًا وفريدًا.',40);return}if(r?.action!=='restore'&&r?.action!=='sync')error.textContent=friendlyAuthError(r?.message);return}if(r.action==='sync')return;if(r.action==='logout'||r.action==='delete'){clearAccountLocal();setAuthMode('login');hideAll();const social=$('social');if(social)social.hidden=true;$('settingsSheet').hidden=true;profile.hidden=false;profile.classList.add('ready');if(r.action==='delete')window.appNotice?.('تم حذف الحساب بنجاح.',{title:'الحساب'});return}if(r.action==='google'||r.action==='login'||r.action==='restore'){if(r.hasProfile&&r.name){store.set('investigator_name',r.name);if(r.username)store.set('investigator_username',r.username);showHQ(r.name)}else{clearAccountLocal();showGoogleSetup(r);if(r.name)$('playerName').value=r.name;if(r.profileConflict)setTimeout(()=>error.textContent='اسم المستخدم السابق مرتبط بحساب آخر. اختر اسم مستخدم جديدًا وفريدًا.',40)}return}if(r.action==='profileSetup'){const name=$('playerName').value.trim(),username=$('playerUsername').value.trim().toLowerCase();store.set('investigator_name',name);store.set('investigator_username',username);pendingGoogleProfile=null;showHQ(name);return}const fallback=$('playerName').value.trim()||'اللاعب',name=(authMode==='create'?fallback:(r.name||fallback)).trim();store.set('investigator_name',name);if(r.username)store.set('investigator_username',r.username);store.set('profile_cache',{name,email:r.email||$('playerEmail').value.trim(),uid:r.uid||''});showHQ(name)};
video.addEventListener('ended',showEnvelope);video.addEventListener('error',()=>{resume.hidden=false;resume.textContent='تعذر تشغيل المقدمة — اضغط للمتابعة';resume.onclick=showEnvelope});video.addEventListener('playing',()=>resume.hidden=true);resume.onclick=()=>video.play();envelope.onclick=openEnvelope;
function validUsername(v){return /^[a-z0-9_]{3,20}$/.test(v)}
$('createProfile').onclick=()=>{if(authBusy)return;const name=$('playerName').value.trim(),username=$('playerUsername').value.trim().replace(/^@/,'').toLowerCase(),email=$('playerEmail').value.trim(),p=$('playerPassword').value;error.textContent='';if(authMode==='googleSetup'){if(name.length<2)return error.textContent='اكتب اسم شخصية من حرفين على الأقل.';if(!validUsername(username))return error.textContent='اسم المستخدم 3–20 حرفًا: إنجليزي، أرقام أو _ فقط.';if(!nativeReady()||typeof NativeAuth.completeProfile!=='function')return error.textContent='حدّث التطبيق لإكمال الهوية.';setAuthBusy(true,'setup');NativeAuth.completeProfile(name,username);return}if(authMode==='create'&&name.length<2)return error.textContent='اكتب اسم شخصية من حرفين على الأقل.';if(authMode==='create'&&!validUsername(username))return error.textContent='اسم المستخدم 3–20 حرفًا: إنجليزي، أرقام أو _ فقط.';if(!/^\S+@\S+\.\S+$/.test(email))return error.textContent='تأكد من البريد الإلكتروني.';if(p.length<8)return error.textContent='كلمة المرور يجب أن تكون 8 أحرف على الأقل.';if(!nativeReady())return error.textContent='خدمة تسجيل الدخول غير متاحة.';setAuthBusy(true,'email');if(authMode==='create')NativeAuth.createAccount(name,username,email,p);else NativeAuth.signInEmail(email,p)};
$('googleAuth').onclick=()=>{if(authBusy)return;if(!nativeReady())return error.textContent='خدمة Google غير متاحة.';authBusy=true;$('googleAuth').disabled=true;NativeAuth.signInGoogle()};
$('existingAccount').onclick=()=>setAuthMode(authMode==='create'?'login':'create');document.querySelectorAll('.case-tab').forEach(b=>b.onclick=()=>setCaseView(b.dataset.view));
$('hqSettings').onclick=()=>$('settingsSheet').hidden=false;$('closeSettings').onclick=()=>$('settingsSheet').hidden=true;$('settingsLogout').onclick=async()=>{if(await window.appConfirm('هل تريد تسجيل الخروج من الحساب؟',{title:'تسجيل الخروج',ok:'تسجيل الخروج'}))NativeAuth?.signOut?.()};$('settingsDelete').onclick=async()=>{if(await window.appConfirm('سيتم حذف حسابك نهائيًا ولا يمكن التراجع عن هذه العملية.',{title:'حذف الحساب',ok:'حذف نهائيًا',danger:true}))NativeAuth?.deleteAccount?.()};
document.addEventListener('click',e=>{const c=e.target.closest?.('.case-card');if(c){store.set('last_case',c.dataset.case);c.animate([{transform:'scale(.985)'},{transform:'scale(1)'}],{duration:100})}});store.set('intro_seen',true);boot();

// Seven-player picture investigation race. Each stage owns its own chat; players only watch their current stage.
(function(){
  const q=s=>document.querySelector(s), game=q('#puzzleGame'), waiting=q('#puzzleWaiting'), arena=q('#puzzleArena'), finished=q('#puzzleFinished');
  if(!game)return;
  let state={matchId:'',stage:1,status:'idle',members:[],names:{},stages:{}};
  function toast(t){const e=q('#puzzleToast');e.textContent=t;e.classList.add('show');setTimeout(()=>e.classList.remove('show'),1800)}
  function setupHQ(){
    const H=q('#hq'); if(!H||H.classList.contains('puzzle-hq'))return; H.classList.add('puzzle-hq');
    const nav=H.querySelector('.hq-nav button');if(nav)nav.textContent='اللعب';
    const tabs=H.querySelector('.case-tabs'),zone=H.querySelector('.case-zone');if(tabs)tabs.hidden=true;if(zone)zone.hidden=true;
    const launch=document.createElement('section');launch.className='puzzle-launch-zone';launch.innerHTML='<div class="puzzle-launch-card"><div class="launch-mark">⌁</div><h1>مساحة التجربة</h1><p>سبعة لاعبين. ست صور. في كل صورة كلمة مفتاحية واحدة. اسأل النظام، ناقش من بقي في مرحلتك، وتقدم قبل الآخرين.</p><button id="startPuzzleMatch" class="puzzle-match-btn"><img class="match-emblem-inline" src="media/investigation-match-emblem.webp" alt=""> ابدأ عيش الدور<small>مطابقة تلقائية مع 6 لاعبين آخرين</small></button></div>';
    const navEl=H.querySelector('.hq-nav');H.insertBefore(launch,navEl||null);q('#startPuzzleMatch').onclick=startMatch;
  }
  setupHQ();document.addEventListener('DOMContentLoaded',setupHQ);
  function openGame(){game.hidden=false;waiting.hidden=false;arena.hidden=true;finished.hidden=true;document.body.style.overflow='hidden'}
  function closeGame(cancel){if(cancel&&state.matchId&&window.NativePuzzle)NativePuzzle.cancelMatch(state.matchId);else if(window.NativePuzzle)NativePuzzle.stop();game.hidden=true;document.body.style.overflow='';state={matchId:'',stage:1,status:'idle',members:[],names:{},stages:{}}}
  function startMatch(){if(!window.NativePuzzle){toast('المطابقة تحتاج نسخة Android.');return}openGame();q('#puzzleWaitingCount').textContent='…';NativePuzzle.matchmake()}
  q('#cancelPuzzleMatch').onclick=()=>closeGame(true);q('#puzzleExit').onclick=()=>{if(state.status==='waiting')closeGame(true);else closeGame(false)};q('#puzzleReturn').onclick=()=>closeGame(false);
  function renderState(){
    const count=state.members.length;q('#puzzlePlayerCount').textContent=count+'/7';q('#puzzleWaitingCount').textContent=count+' / 7';
    if(state.status==='waiting'){waiting.hidden=false;arena.hidden=true;finished.hidden=true;return}
    waiting.hidden=true;
    if(state.stage>6){arena.hidden=true;finished.hidden=false;q('#puzzleStageLabel').textContent='اكتمل 6 / 6';return}
    arena.hidden=false;finished.hidden=true;q('#puzzleStageLabel').textContent='المرحلة '+state.stage+' / 6';q('#puzzleImage').src='puzzle-'+state.stage+'.svg';q('#puzzleProgressBar').style.width=((state.stage/6)*100)+'%';
    const peers=state.members.filter(id=>Number(state.stages[id]||1)===state.stage).length;q('#stagePeersText').textContent=peers===1?'أنت وحدك في هذه المرحلة':peers+' لاعبين في هذه المرحلة';
  }
  function clearForStage(){q('#puzzleSystemFeed').innerHTML='<div class="system-note">بدأت مرحلة جديدة. أسئلة هذه المرحلة خاصة بك.</div>';q('#puzzleStageChat').innerHTML='';q('#puzzleQuestion').value='';q('#puzzleChatInput').value='';q('#puzzleSolution').value='';q('#puzzleSolveMsg').textContent=''}
  window.onNativePuzzleEvent=function(ev){
    if(!ev||!ev.ok){if(ev&&ev.message)toast(ev.message);return}
    if(ev.action==='state'){const old=state.stage;state.matchId=ev.matchId;state.status=ev.status;state.members=ev.members||[];state.names=ev.names||{};state.stages=ev.stages||{};state.stage=Number(ev.stage||1);if(old!==state.stage)clearForStage();renderState();return}
    if(ev.action==='answer'){const feed=q('#puzzleSystemFeed');const a=document.createElement('div');a.className='qa-line';a.textContent='أنت: '+ev.question;feed.appendChild(a);const b=document.createElement('div');b.className='qa-line answer';b.innerHTML='<b>النظام:</b> '+ev.answer;feed.appendChild(b);feed.scrollTop=feed.scrollHeight;return}
    if(ev.action==='chat'&&Number(ev.stage)===state.stage){const box=q('#puzzleStageChat');const arr=[...(ev.messages||[])].sort((a,b)=>(a.time||0)-(b.time||0));box.innerHTML=arr.length?'':'<div class="system-note">لا توجد رسائل بعد.</div>';arr.forEach(m=>{const d=document.createElement('div');d.className='chat-line';d.innerHTML='<small></small><span></span>';d.querySelector('small').textContent=m.name||'لاعب';d.querySelector('span').textContent=m.text||'';box.appendChild(d)});box.scrollTop=box.scrollHeight;return}
    if(ev.action==='solution'){const m=q('#puzzleSolveMsg');if(ev.correct){m.className='puzzle-solve-ok';m.textContent=Number(ev.stage)===6?'✓ الكلمة السادسة صحيحة. أنهيت عيش الدور!':'✓ صحيح — انتقلت للمرحلة التالية.';q('#puzzleSolution').value=''}else{m.className='puzzle-solve-bad';m.textContent='ليست الكلمة المطلوبة. أكمل عيش الدور.'}return}
  };
  q('#puzzleAsk').onclick=()=>{const v=q('#puzzleQuestion').value.trim();if(!v)return;if(!state.matchId||state.stage>6)return;NativePuzzle.ask(state.matchId,state.stage,v);q('#puzzleQuestion').value=''};
  q('#puzzleQuestion').addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();q('#puzzleAsk').click()}});
  q('#puzzleChatSend').onclick=()=>{const v=q('#puzzleChatInput').value.trim();if(!v||!state.matchId)return;NativePuzzle.sendStageChat(state.matchId,state.stage,v);q('#puzzleChatInput').value=''};
  q('#puzzleChatInput').addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();q('#puzzleChatSend').click()}});
  q('#puzzleSolve').onclick=()=>{const v=q('#puzzleSolution').value.trim();if(!v||!state.matchId)return;NativePuzzle.submitSolution(state.matchId,state.stage,v)};
  q('#puzzleSolution').addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();q('#puzzleSolve').click()}});
})();
